import React, { useState } from 'react'

const View = () => {
  return (
    var[update,setupdate]=useState(false)
    var[update,setselected]=useState([])
    var[students,setstudents=useState([])

    
      
    
  )
}

export default View
